# used for bucket names and dynamo tables | must be globally unique across all AWS accounts.
name           = "scale-solutions"
aws_account_id = "975050022359"  # Replace with Dev account ID
environment    = "dev"
primary_region = "us-east-1"